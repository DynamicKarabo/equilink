Prompt 1: Multi-Asset Support & Regulatory Amendments Expand EquiLink's domain model to support equities, ETFs, and futures, implementing the different tick rules and margin treatments required for each asset class. Additionally, implement the OrderCorrectedEvent. Since our event store is strictly append-only by database-level constraints, correcting genuine data errors requires this compensating event for the regulatory amendment workflow, rather than direct mutation.
Prompt 2: Compliance Audit & WORM Archival Build the compliance audit export endpoint (GET /audit/orders?from=&to=) that queries the event store's JSONB payloads and generates a signed CSV/PDF. Then, implement the automated monthly partition archival process. This job must export the monthly PostgreSQL partitions of the order_events table to Azure Blob Storage using a WORM (Write Once Read Many) policy to satisfy our 7-year regulatory retention requirements.
Prompt 3: Multi-Region Scaling & Read Replicas Provision a multi-region PostgreSQL read replica. Update the Data Tier Architecture to implement read replica routing for all Dapper queries. Ensure that the high-volume Dapper read queries are directed to these replicas via connection string switching, while all Entity Framework Core write operations continue to go exclusively to the primary instance.
Prompt 4: Fund Onboarding API Implement the Fund Onboarding API by creating the POST /funds endpoint. This must include risk limit templating, allowing administrators to dynamically configure the initial MaxOrderSize, DailyLossLimit, and ConcentrationLimit thresholds in the PostgreSQL database before the fund starts trading.
Prompt 5: Addressing Unresolved Tradeoffs (Note to LLM: Act as an architecture advisory prompt) Evaluate and implement the final unresolved architectural decisions:

    Establish the aggregate snapshot strategy (e.g., every 50 events vs time-based) to reduce reconstruction latency for long-lived orders.
    Determine if the projections should remain synchronous (in-process) or move to asynchronous (via Azure Service Bus) based on the results of the Phase 4 performance load tests.
    Outline the transition plan for multi-region active-passive vs active-active failover, specifically addressing distributed locking on the Redis risk state.

Once these are complete, EquiLink is fully mature, compliance-ready, and capable of multi-region scale!